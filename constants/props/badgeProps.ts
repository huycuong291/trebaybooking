import { BadgeVariant } from '@mantine/core';
import { GRADIENTS } from '@/constants/theme/color';

export const gradientBadgeProps = {
  variant: 'gradient' as BadgeVariant,
  gradient: GRADIENTS.TEAL_LIME_105
};
