import { createStyles } from '@mantine/core';

export const useBookingInforStyles = createStyles((theme) => ({
  textInput: {
    borderTop: 'unset',
    borderLeft: 'unset',
    borderRight: 'unset',
    borderRadius: 'unset',
    borderBottomWidth: 2
  }
}));
